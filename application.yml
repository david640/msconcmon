spring:
  zipkin:
    base-url: http://127.0.0.1:9411/
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: "jdbc:postgresql://localhost:5432/sbtest"
    username: postgres
    password: dheyys!!Z34
testmessage:
 this is a test message from the config server
